# תוצאות אימות — חילוץ core

בסיס להשוואה: `maakev-production-warning-baseline`.

## תוצאה

- 25 תרחישי חוזה עוברים על ה־core החדש בקריאה ישירה.
- 25 תרחישים עוברים גם בהרצה דרך הקובץ הראשי החדש.
- 0 הבדלים מתוך 25 מול הבסיס הקפוא: גם אובייקט התוצאה וגם דפוס קריאות הרשת זהים.
- 0 הבדלים מתוך 25 בין ה־core הישיר לבין הקובץ הראשי החדש.
- `Gadget page tool.card.js`, `Gadget page tool.messages.js` ו־`Gadget page tool.css` לא השתנו.
- כל קבצי JavaScript עוברים בדיקת תחביר.

## שינוי במריץ

המריץ יודע כעת לעבוד בשני מצבים:

- גרסה ישנה: מריץ את הקובץ הראשי ומזריק נקודות לכידה בזיכרון.
- גרסה עם `Gadget page tool.core.js`: קורא ישירות לליבה.

בנוסף, כשלי תרחישים נאספים ומודפסים יחד במקום לעצור בכשל הכוונה הראשון.

## עדיין לא נבדק

בדיקה חיה באתר אמיתי. היא נשארת תנאי לפני החלפת גרסת הייצור.
